#Load Libraries
library(randomForest)
library(caret)
library(ranger)
library(pROC)
library(boot)
library(ggplot2)
library(phyloseq)
library(tidyverse)
# Load the dataset and aggregate reads to the Genus level
load("with_inflammed_ryan_filt.RData")
# Load the dataset and aggregate reads to the Genus level
inflammed_metadata <- read.delim("ryan_metadata.tsv", sep = "\t")
# Selecting the metadata columns + outcomes
df <- metadata %>%
select(Condition, Age_when_sampled, Biopsy_location, Smoking.status, Gender) %>%
# Apostrophes causes errors in R
mutate(Condition = str_replace_all(Condition, "Crohn's Disease", "Crohns_Disease")) %>%
# Filter to exclude UC
filter(Condition %in% c("Crohns_Disease", "Healthy"))
# Selecting the metadata columns + outcomes
df <- inflammed_metadata %>%
select(Condition, Age_when_sampled, Biopsy_location, Smoking.status, Gender) %>%
# Apostrophes causes errors in R
mutate(Condition = str_replace_all(Condition, "Crohn's Disease", "Crohns_Disease")) %>%
# Filter to exclude UC
filter(Condition %in% c("Crohns_Disease", "Healthy"))
# Remove rows with NA values
df_noNA <- df %>% na.omit()
#Set Predictors
predictors <- df_noNA %>%
select(-Condition) %>%
# Need to change categorical variables to factors
mutate(
Biopsy_location = as.factor(Biopsy_location),
Smoking.status = as.factor(Smoking.status),
Gender = as.factor(Gender),
Age_when_sampled = as.numeric(Age_when_sampled))
# Set Outcomes
outcome <- df_noNA %>% pull(Condition) %>%
factor(levels = c("Healthy", "Crohns_Disease"))
# Randomly subsets the rows into k equal bins.
k = 10
set.seed(060)
folds = createFolds(outcome, k = k, list = TRUE)
# Each of these folds will take a turn being the test dataset.
str(folds)
# These are generic values. Depending on your dataset, you may need to adjust the numeric ranges up or down.
tune_grid = expand.grid(mtry = c(5,7,10),
splitrule = c("gini","extratrees"),
min.node.size = c(21,42,83))
#Run RF
source('randomforest_functions.R')
clinical_model <- run_rf(
X = predictors,
y = outcome,
fold_list = folds,
hyper = tune_grid,
rngseed = 060)
# These are generic values. Depending on your dataset, you may need to adjust the numeric ranges up or down.
tune_grid = expand.grid(mtry = c(1,2,3,4),
splitrule = c("gini","extratrees"),
min.node.size = c(21,42,83))
#Run RF
source('randomforest_functions.R')
clinical_model <- run_rf(
X = predictors,
y = outcome,
fold_list = folds,
hyper = tune_grid,
rngseed = 060)
# Remove rows with NA values
df_noNA <- df %>% na.omit()
# Set Predictors
predictors <- df_noNA %>%
select(-Condition) %>%
# Need to change categorical variables to factors
mutate(
Biopsy_location = as.factor(Biopsy_location),
Smoking.status = as.factor(Smoking.status),
Gender = as.factor(Gender),
Age_when_sampled = as.numeric(Age_when_sampled))
unique(metadata$Age_when_sampled)
unique(inflammed_metadata$Age_when_sampled)
# Selecting the metadata columns + outcomes
df <- inflammed_metadata %>%
select(Condition, Age_when_sampled, Biopsy_location, Smoking.status, Gender) %>%
# Apostrophes causes errors in R
mutate(Condition = str_replace_all(Condition, "Crohn's Disease", "Crohns_Disease")) %>%
# Filter to exclude UC
filter(Condition %in% c("Crohns_Disease", "Healthy")) %>%
# Have to change Age_when_sampled to factor before the others
# as it introduces new NAs if done after the actual NA removal step
mutate(Age_when_sampled = as.numeric(Age_when_sampled))
# Remove rows with NA values
df_noNA <- df %>% na.omit()
nrow(df)
nrow(df_noNA)
inflammed_metadata %>% filter(Age_when_sampled == "Uncertain")
# Set Predictors
predictors <- df_noNA %>%
select(-Condition) %>%
# Need to change categorical variables to factors
mutate(
Biopsy_location = as.factor(Biopsy_location),
Smoking.status = as.factor(Smoking.status),
Gender = as.factor(Gender),
Age_when_sampled = as.numeric(Age_when_sampled))
# Set Outcomes
outcome <- df_noNA %>% pull(Condition) %>%
factor(levels = c("Healthy", "Crohns_Disease"))
# Randomly subsets the rows into k equal bins.
k = 10
set.seed(060)
folds = createFolds(outcome, k = k, list = TRUE)
# Each of these folds will take a turn being the test dataset.
str(folds)
# These are generic values. Depending on your dataset, you may need to adjust the numeric ranges up or down.
tune_grid = expand.grid(mtry = c(1,2,3,4),
splitrule = c("gini","extratrees"),
min.node.size = c(21,42,83))
#Run RF
source('randomforest_functions.R')
clinical_model <- run_rf(
X = predictors,
y = outcome,
fold_list = folds,
hyper = tune_grid,
rngseed = 060)
names(clinical_model)
names(clinical_model)
roc_test = roc(clinical_model$test_labels$true_labels,
clinical_model$test_labels$predicted_probabilities)
roc_train = roc(clinical_model$train_labels$true_labels,
clinical_model$train_labels$predicted_probabilities)
ggplot() +
# Training data: this is a type of control
geom_line(aes(x = 1 - roc_train$specificities,
y = roc_train$sensitivities),
color = "red",size=1) +
# Test data: tells us the strength of the prediction
geom_line(aes(x = 1 - roc_test$specificities,
y = roc_test$sensitivities),
color = "black",size=1) +
geom_abline(slope = 1, intercept = 0, color = "gray", linetype = "dashed",size=1) +
labs(x = "False Positive Rate", y = "True Positive Rate",
title = "ROC curve - Clinical Variables Model including inflammed") +
annotate("text", x = 0.7, y = 0.2,
label = sprintf("Train (red): %.2f (%.2f-%.2f)\nTest (black): %.2f (%.2f-%.2f)",
auc(roc_train), clinicla_model$auc_train_ci[1], clinical_model$auc_train_ci[2],
auc(roc_test), clinical_model$auc_test_ci[1], clinical_model$auc_test_ci[2]),
size = 6) +
theme_minimal(base_size=18)
ggplot() +
# Training data: this is a type of control
geom_line(aes(x = 1 - roc_train$specificities,
y = roc_train$sensitivities),
color = "red",size=1) +
# Test data: tells us the strength of the prediction
geom_line(aes(x = 1 - roc_test$specificities,
y = roc_test$sensitivities),
color = "black",size=1) +
geom_abline(slope = 1, intercept = 0, color = "gray", linetype = "dashed",size=1) +
labs(x = "False Positive Rate", y = "True Positive Rate",
title = "ROC curve - Clinical Variables Model including inflammed") +
annotate("text", x = 0.7, y = 0.2,
label = sprintf("Train (red): %.2f (%.2f-%.2f)\nTest (black): %.2f (%.2f-%.2f)",
auc(roc_train), clinical_model$auc_train_ci[1], clinical_model$auc_train_ci[2],
auc(roc_test), clinical_model$auc_test_ci[1], clinical_model$auc_test_ci[2]),
size = 6) +
theme_minimal(base_size=18)
clinical_model$importance
clinical_model$importance %>%
# Data are automatically arranged by decreasing importance - turn it into a factor.
# Otherwise the features will show up alphabetically in the plot.
mutate(Feature = factor(.$Feature,levels = .$Feature)) %>%
ggplot(aes(Feature,MeanDecreaseGini,fill=MeanDecreaseGini)) +
geom_col() +
theme_classic(base_size=18) +
theme(axis.text.x = element_text(angle=45, vjust = 1, hjust=1)) +
ylab('Importance (Gini)') + xlab(NULL)
clinical_model$importance %>%
# Data are automatically arranged by decreasing importance - turn it into a factor.
# Otherwise the features will show up alphabetically in the plot.
mutate(Feature = factor(.$Feature,levels = .$Feature)) %>%
ggplot(aes(Feature,MeanDecreaseGini,fill=MeanDecreaseGini)) +
geom_col() +
theme_classic(base_size=18) +
theme(axis.text.x = element_text(angle=45, vjust = 1, hjust=1)) +
ylab('Importance (Gini)') + xlab(NULL) +ggtitle("Variable Importance - Clinical Model with inflammed")
clinical_model$importance %>%
# Data are automatically arranged by decreasing importance - turn it into a factor.
# Otherwise the features will show up alphabetically in the plot.
mutate(Feature = factor(.$Feature,levels = .$Feature)) %>%
ggplot(aes(Feature,MeanDecreaseGini,fill=MeanDecreaseGini)) +
geom_col() +
theme_classic(base_size=18) +
theme(axis.text.x = element_text(angle=45, vjust = 1, hjust=1),
plot.margin = margin(10, 30, 10, 10)) +
ylab('Importance (Gini)') + xlab(NULL)
clinical_model$importance %>%
# Data are automatically arranged by decreasing importance - turn it into a factor.
# Otherwise the features will show up alphabetically in the plot.
mutate(Feature = factor(.$Feature,levels = .$Feature)) %>%
ggplot(aes(Feature,MeanDecreaseGini,fill=MeanDecreaseGini)) +
geom_col() +
theme_classic(base_size=18) +
theme(axis.text.x = element_text(angle=45, vjust = 1, hjust=1),
plot.margin = margin(10, 30, 80, 10)) +
ylab('Importance (Gini)') + xlab(NULL) +  coord_cartesian(clip = "off")
clinical_model$importance %>%
# Data are automatically arranged by decreasing importance - turn it into a factor.
# Otherwise the features will show up alphabetically in the plot.
mutate(Feature = factor(.$Feature,levels = .$Feature)) %>%
ggplot(aes(Feature,MeanDecreaseGini,fill=MeanDecreaseGini)) +
geom_col() +
theme_classic(base_size=18) +
theme(axis.text.x = element_text(angle=45, vjust = 1, hjust=1),
plot.margin = margin(10, 30, 80, 10)) +
ylab('Importance (Gini)') + xlab(NULL)
clinical_model$importance %>%
# Data are automatically arranged by decreasing importance - turn it into a factor.
# Otherwise the features will show up alphabetically in the plot.
mutate(Feature = factor(.$Feature,levels = .$Feature)) %>%
ggplot(aes(Feature,MeanDecreaseGini,fill=MeanDecreaseGini)) +
geom_col() +
theme_classic(base_size=18) +
theme(axis.text.x = element_text(angle=45, vjust = 1, hjust=1),
plot.margin = margin(10, 30, 50, 10)) +
ylab('Importance (Gini)') + xlab(NULL)
clinical_model$importance %>%
# Data are automatically arranged by decreasing importance - turn it into a factor.
# Otherwise the features will show up alphabetically in the plot.
mutate(Feature = factor(.$Feature,levels = .$Feature)) %>%
ggplot(aes(Feature,MeanDecreaseGini,fill=MeanDecreaseGini)) +
geom_col() +
theme_classic(base_size=18) +
theme(axis.text.x = element_text(angle=45, vjust = 1, hjust=1),
plot.margin = margin(10, 30, 40, 10)) +
ylab('Importance (Gini)') + xlab(NULL)
clinical_model$importance %>%
# Data are automatically arranged by decreasing importance - turn it into a factor.
# Otherwise the features will show up alphabetically in the plot.
mutate(Feature = factor(.$Feature,levels = .$Feature)) %>%
ggplot(aes(Feature,MeanDecreaseGini,fill=MeanDecreaseGini)) +
geom_col() +
theme_classic(base_size=18) +
theme(axis.text.x = element_text(angle=45, vjust = 1, hjust=1),
plot.margin = margin(10, 30, 20, 10)) +
ylab('Importance (Gini)') + xlab(NULL)
clinical_model$importance %>%
# Data are automatically arranged by decreasing importance - turn it into a factor.
# Otherwise the features will show up alphabetically in the plot.
mutate(Feature = factor(.$Feature,levels = .$Feature)) %>%
ggplot(aes(Feature,MeanDecreaseGini,fill=MeanDecreaseGini)) +
geom_col() +
theme_classic(base_size=18) +
theme(axis.text.x = element_text(angle=45, vjust = 1, hjust=1),
plot.margin = margin(10, 30, 10, 10)) +
ylab('Importance (Gini)') + xlab(NULL)
clinical_model$importance %>%
# Data are automatically arranged by decreasing importance - turn it into a factor.
# Otherwise the features will show up alphabetically in the plot.
mutate(Feature = factor(.$Feature,levels = .$Feature)) %>%
ggplot(aes(Feature,MeanDecreaseGini,fill=MeanDecreaseGini)) +
geom_col() +
theme_classic(base_size=18) +
theme(axis.text.x = element_text(angle=45, vjust = 1, hjust=1),
plot.margin = margin(10, 10, 10, 10)) +
ylab('Importance (Gini)') + xlab(NULL)
clinical_model$importance %>%
# Data are automatically arranged by decreasing importance - turn it into a factor.
# Otherwise the features will show up alphabetically in the plot.
mutate(Feature = factor(.$Feature,levels = .$Feature)) %>%
ggplot(aes(Feature,MeanDecreaseGini,fill=MeanDecreaseGini)) +
geom_col() +
theme_classic(base_size=18) +
theme(axis.text.x = element_text(angle=45, vjust = 1, hjust=1),
plot.margin = margin(20, 10, 10, 10)) +
ylab('Importance (Gini)') + xlab(NULL)
clinical_model$importance %>%
# Data are automatically arranged by decreasing importance - turn it into a factor.
# Otherwise the features will show up alphabetically in the plot.
mutate(Feature = factor(.$Feature,levels = .$Feature)) %>%
ggplot(aes(Feature,MeanDecreaseGini,fill=MeanDecreaseGini)) +
geom_col() +
theme_classic(base_size=18) +
theme(axis.text.x = element_text(angle=45, vjust = 1, hjust=1),
plot.margin = margin(25, 10, 10, 10)) +
ylab('Importance (Gini)') + xlab(NULL)
clinical_model$importance %>%
# Data are automatically arranged by decreasing importance - turn it into a factor.
# Otherwise the features will show up alphabetically in the plot.
mutate(Feature = factor(.$Feature,levels = .$Feature)) %>%
ggplot(aes(Feature,MeanDecreaseGini,fill=MeanDecreaseGini)) +
geom_col() +
theme_classic(base_size=18) +
theme(axis.text.x = element_text(angle=45, vjust = 1, hjust=1),
plot.margin = margin(40, 10, 10, 10)) +
ylab('Importance (Gini)') + xlab(NULL)
clinical_model$importance %>%
# Data are automatically arranged by decreasing importance - turn it into a factor.
# Otherwise the features will show up alphabetically in the plot.
mutate(Feature = factor(.$Feature,levels = .$Feature)) %>%
ggplot(aes(Feature,MeanDecreaseGini,fill=MeanDecreaseGini)) +
geom_col() +
theme_classic(base_size=18) +
theme(axis.text.x = element_text(angle=45, vjust = 1, hjust=1),
plot.margin = margin(10, 10, 10, 10)) +
ylab('Importance (Gini)') + xlab(NULL)
clinical_model$importance %>%
# Data are automatically arranged by decreasing importance - turn it into a factor.
# Otherwise the features will show up alphabetically in the plot.
mutate(Feature = factor(.$Feature,levels = .$Feature)) %>%
ggplot(aes(Feature,MeanDecreaseGini,fill=MeanDecreaseGini)) +
geom_col() +
theme_classic(base_size=18) +
theme(axis.text.x = element_text(angle=45, vjust = 1, hjust=1),
plot.margin = margin(10, 40, 10, 10)) +
ylab('Importance (Gini)') + xlab(NULL)
clinical_model$importance %>%
# Data are automatically arranged by decreasing importance - turn it into a factor.
# Otherwise the features will show up alphabetically in the plot.
mutate(Feature = factor(.$Feature,levels = .$Feature)) %>%
ggplot(aes(Feature,MeanDecreaseGini,fill=MeanDecreaseGini)) +
geom_col() +
theme_classic(base_size=18) +
theme(axis.text.x = element_text(angle=45, vjust = 1, hjust=1),
plot.margin = margin(10, 10, 10, 10)) +
ylab('Importance (Gini)') + xlab(NULL)
clinical_model$importance %>%
# Data are automatically arranged by decreasing importance - turn it into a factor.
# Otherwise the features will show up alphabetically in the plot.
mutate(Feature = factor(.$Feature,levels = .$Feature)) %>%
ggplot(aes(Feature,MeanDecreaseGini,fill=MeanDecreaseGini)) +
geom_col() +
theme_classic(base_size=18) +
theme(axis.text.x = element_text(angle=45, vjust = 1, hjust=1),
plot.margin = margin(10, 0, 10, 10)) +
ylab('Importance (Gini)') + xlab(NULL)
clinical_model$importance %>%
# Data are automatically arranged by decreasing importance - turn it into a factor.
# Otherwise the features will show up alphabetically in the plot.
mutate(Feature = factor(.$Feature,levels = .$Feature)) %>%
ggplot(aes(Feature,MeanDecreaseGini,fill=MeanDecreaseGini)) +
geom_col() +
theme_classic(base_size=18) +
theme(axis.text.x = element_text(angle=45, vjust = 1, hjust=1),
plot.margin = margin(10, 0, 40, 10)) +
ylab('Importance (Gini)') + xlab(NULL)
clinical_model$importance %>%
# Data are automatically arranged by decreasing importance - turn it into a factor.
# Otherwise the features will show up alphabetically in the plot.
mutate(Feature = factor(.$Feature,levels = .$Feature)) %>%
ggplot(aes(Feature,MeanDecreaseGini,fill=MeanDecreaseGini)) +
geom_col() +
theme_classic(base_size=18) +
theme(axis.text.x = element_text(angle=45, vjust = 1, hjust=1),
plot.margin = margin(10, 0, 10, 10)) +
ylab('Importance (Gini)') + xlab(NULL)
clinical_model$importance %>%
# Data are automatically arranged by decreasing importance - turn it into a factor.
# Otherwise the features will show up alphabetically in the plot.
mutate(Feature = factor(.$Feature,levels = .$Feature)) %>%
ggplot(aes(Feature,MeanDecreaseGini,fill=MeanDecreaseGini)) +
geom_col() +
theme_classic(base_size=18) +
theme(axis.text.x = element_text(angle=45, vjust = 1, hjust=1),
plot.margin = margin(10, 0, 10, 4)) +
ylab('Importance (Gini)') + xlab(NULL)
clinical_model$importance %>%
# Data are automatically arranged by decreasing importance - turn it into a factor.
# Otherwise the features will show up alphabetically in the plot.
mutate(Feature = factor(.$Feature,levels = .$Feature)) %>%
ggplot(aes(Feature,MeanDecreaseGini,fill=MeanDecreaseGini)) +
geom_col() +
theme_classic(base_size=18) +
theme(axis.text.x = element_text(angle=45, vjust = 1, hjust=1),
plot.margin = margin(10, 0, 10, 40)) +
ylab('Importance (Gini)') + xlab(NULL)
clinical_model$importance %>%
# Data are automatically arranged by decreasing importance - turn it into a factor.
# Otherwise the features will show up alphabetically in the plot.
mutate(Feature = factor(.$Feature,levels = .$Feature)) %>%
ggplot(aes(Feature,MeanDecreaseGini,fill=MeanDecreaseGini)) +
geom_col() +
theme_classic(base_size=18) +
theme(axis.text.x = element_text(angle=45, vjust = 1, hjust=1),
plot.margin = margin(10, 0, 10, 20)) +
ylab('Importance (Gini)') + xlab(NULL)
clinical_model$importance %>%
# Data are automatically arranged by decreasing importance - turn it into a factor.
# Otherwise the features will show up alphabetically in the plot.
mutate(Feature = factor(.$Feature,levels = .$Feature)) %>%
ggplot(aes(Feature,MeanDecreaseGini,fill=MeanDecreaseGini)) +
geom_col() +
theme_classic(base_size=18) +
theme(axis.text.x = element_text(angle=45, vjust = 1, hjust=1),
plot.margin = margin(10, 0, 10, 10)) +
ylab('Importance (Gini)') + xlab(NULL)
ggplot() +
# Training data: this is a type of control
geom_line(aes(x = 1 - roc_train$specificities,
y = roc_train$sensitivities),
color = "red",size=1) +
# Test data: tells us the strength of the prediction
geom_line(aes(x = 1 - roc_test$specificities,
y = roc_test$sensitivities),
color = "black",size=1) +
geom_abline(slope = 1, intercept = 0, color = "gray", linetype = "dashed",size=1) +
labs(x = "False Positive Rate", y = "True Positive Rate",
title = "ROC curve - Clinical Variables Model including inflammed") +
annotate("text", x = 0.7, y = 0.2,
label = sprintf("Train (red): %.2f (%.2f-%.2f)\nTest (black): %.2f (%.2f-%.2f)",
auc(roc_train), clinical_model$auc_train_ci[1], clinical_model$auc_train_ci[2],
auc(roc_test), clinical_model$auc_test_ci[1], clinical_model$auc_test_ci[2]),
size = 6) +
theme_minimal(base_size=18)
ggplot() +
# Training data: this is a type of control
geom_line(aes(x = 1 - roc_train$specificities,
y = roc_train$sensitivities),
color = "red",size=1) +
# Test data: tells us the strength of the prediction
geom_line(aes(x = 1 - roc_test$specificities,
y = roc_test$sensitivities),
color = "black",size=1) +
geom_abline(slope = 1, intercept = 0, color = "gray", linetype = "dashed",size=1) +
labs(x = "False Positive Rate", y = "True Positive Rate",
title = "Clinical Variables Model with inflammed") +
annotate("text", x = 0.7, y = 0.2,
label = sprintf("Train (red): %.2f (%.2f-%.2f)\nTest (black): %.2f (%.2f-%.2f)",
auc(roc_train), clinical_model$auc_train_ci[1], clinical_model$auc_train_ci[2],
auc(roc_test), clinical_model$auc_test_ci[1], clinical_model$auc_test_ci[2]),
size = 6) +
theme_minimal(base_size=18)
ggplot() +
# Training data: this is a type of control
geom_line(aes(x = 1 - roc_train$specificities,
y = roc_train$sensitivities),
color = "red",size=1) +
# Test data: tells us the strength of the prediction
geom_line(aes(x = 1 - roc_test$specificities,
y = roc_test$sensitivities),
color = "black",size=1) +
geom_abline(slope = 1, intercept = 0, color = "gray", linetype = "dashed",size=1) +
labs(x = "False Positive Rate", y = "True Positive Rate",
title = "Clinical Variables Model w inflammed") +
annotate("text", x = 0.7, y = 0.2,
label = sprintf("Train (red): %.2f (%.2f-%.2f)\nTest (black): %.2f (%.2f-%.2f)",
auc(roc_train), clinical_model$auc_train_ci[1], clinical_model$auc_train_ci[2],
auc(roc_test), clinical_model$auc_test_ci[1], clinical_model$auc_test_ci[2]),
size = 6) +
theme_minimal(base_size=18)
